server:
  port: 5050
spring:
  application:
    name: manager-service
  datasource:
    url: jdbc:db-url
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
  properties:
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: yourSecret
