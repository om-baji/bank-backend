server:
  port: 4000
spring:
  application:
    name: user-service
  datasource:
    url: jdbc:db-url
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
  properties:
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect

admin:
  secret: mySecretKey

jwt:
  secret: yourSecret
